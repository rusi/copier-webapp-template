{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Backend",
            "type": "shell",
            "command": "pdm run uvicorn app.main:app --reload",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
